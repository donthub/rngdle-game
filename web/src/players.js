export const PLAYER_KEYS = Object.freeze(["p1", "p2"]);

// The tug band shows names whole, so it reserves room for one this long ($tug-identity-max)
export const PLAYER_NAME_MAX_LENGTH = 64;

export const PLAYER_LABELS = Object.freeze({
    p1: "Player 1",
    p2: "Player 2",
});
